---
phase: 07-code-organization
plan: 03
type: execute
---

<objective>
Verify naming conventions compliance across the entire codebase.

Purpose: Ensure consistent naming patterns following Dart style guide and project conventions, improving code predictability and maintainability.
Output: All code follows established naming conventions with documented style guide.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

# Auto-selected based on dependency graph (from frontmatter):
@.planning/phases/02-constants-config/02-01-SUMMARY.md

# Key files from frontmatter (relevant to this phase):
@lib/**/*.dart

**Tech stack available:** dart, flutter, dart analyze
**Established patterns:**
- Files: snake_case.dart
- Classes: PascalCase
- Functions/Methods/Variables: camelCase
- Private members: _prefix
- Constants: lowerCamelCase (private static final)

**Constraining decisions:**
- [Phase 2-01]: Use lowerCamelCase naming for constants (consistent pattern)
- [Phase 7-01]: Imports organized (prior plan)
- [Phase 7-02]: Unused code removed (prior plan)

**Issues being addressed:** Naming convention consistency (code cleanup)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit naming conventions across all files</name>
  <files>lib/**/*.dart</files>
  <action>Systematically check naming conventions:

**File names:**
- All files should be `snake_case.dart`
- Screen files: `[feature]_screen.dart`
- Service files: `[service]_service.dart`
- Provider files: `[entity]_provider.dart`
- Widget files: `[widget_name].dart`

**Class names:**
- Should be `PascalCase`
- Examples: `TransactionProvider`, `HomeScreen`, `FinanceCalculator`

**Method/Function names:**
- Should be `camelCase`
- Examples: `calculateStatus()`, `getAllTransactions()`

**Variable names:**
- Should be `camelCase`
- Examples: `monthlyIncome`, `totalSpending`

**Private members:**
- Should have `_` prefix
- Examples: `_currentTheme`, `_loadSettings()`

**Constants:**
- Most are `private static final` with lowerCamelCase
- Examples: `FinanceConfig.overBudgetPenalty`

Document any violations found.</action>
  <verify>List of naming convention violations generated (if any)</verify>
  <done>Complete audit report of naming convention compliance</done>
</task>

<task type="auto">
  <name>Task 2: Fix any naming convention violations</name>
  <files>lib/**/*.dart</files>
  <action>Fix any naming violations found in Task 1:

**If violations exist:**
1. Rename files/variables/classes to match conventions
2. Update all references across the codebase
3. Run `dart fix --apply` to auto-fix simple issues
4. Run tests to ensure nothing broke

**Common fixes:**
- File names: `myClass.dart` → `my_class.dart`
- Class names: `myClass` → `MyClass`
- Method names: `MyMethod` → `myMethod`
- Constants: `MY_CONSTANT` → `myConstant` (if public static)

**If no violations found:** Document that codebase already follows conventions.

DO NOT change:
- Public API names (would be breaking change)
- Names that match domain language exactly
- Test file names (should match source file)</action>
  <verify>`dart analyze` passes, all tests pass, no naming violations remain</verify>
  <done>All naming follows Dart style guide and project conventions</done>
</task>

<task type="auto">
  <name>Task 3: Update documentation with naming standards</name>
  <files>.planning/codebase/CONVENTIONS.md</files>
  <action>Ensure CONVENTIONS.md documents current naming standards:

1. Add any naming patterns discovered during audit
2. Clarify any ambiguous conventions
3. Add examples from actual codebase
4. Document any exceptions to standard rules

If CONVENTIONS.md is already accurate, no changes needed.

This documentation helps future developers understand the patterns.</action>
  <verify>CONVENTIONS.md accurately reflects current codebase naming patterns</verify>
  <done>Naming conventions documented and accessible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All files follow snake_case naming
- [ ] All classes use PascalCase
- [ ] All methods/variables use camelCase
- [ ] Private members have _ prefix
- [ ] Constants follow project convention
- [ ] `dart analyze` passes
- [ ] All tests pass
- [ ] CONVENTIONS.md updated if needed
</verification>

<success_criteria>

- Codebase follows consistent naming conventions
- No violations of Dart style guide
- Documentation reflects actual patterns
- All functionality preserved
  </success_criteria>

<output>
After completion, create `.planning/phases/07-code-organization/07-03-SUMMARY.md`:

# Phase 7 Plan 3: Naming Conventions Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.dart` - Description
- `path/to/another.dart` - Description
- `.planning/codebase/CONVENTIONS.md` - Updated documentation

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 7 Complete - Ready for Phase 8 (Final Verification)
</output>
