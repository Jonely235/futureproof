---
phase: 01-logging-foundation
type: execute
---

<objective>
Replace print statements in service layer with structured logging.

Purpose: Replace inconsistent print statements with AppLogger utility calls in all service files for better debugging and production monitoring.
Output: Service layer using structured logging throughout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./01-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@lib/utils/app_logger.dart
@lib/services/database_service.dart
@lib/services/analytics_service.dart
@lib/services/backup_service.dart
@lib/services/finance_calculator.dart
@docs/GUIDES/logging_guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace print statements in DatabaseService</name>
  <files>lib/services/database_service.dart</files>
  <action>Replace all print() statements with AppLogger calls:
1. Import AppLogger
2. Replace emoji-prefixed prints (📁, 📊, ✅, ❌) with appropriate AppLogger methods
3. Keep the emoji prefixes in messages for visual clarity
4. Use SEVERE for errors, INFO for successful operations

Example pattern:
- OLD: print('✅ Added transaction ${transaction.id}')
- NEW: AppLogger.database.info('✅ Added transaction ${transaction.id}')

DO NOT change the logic or remove information - only replace print() with proper logging calls.</action>
  <verify>File contains no print() statements, all replaced with AppLogger calls</verify>
  <done>DatabaseService uses AppLogger.database for all logging, no print statements remain</done>
</task>

<task type="auto">
  <name>Task 2: Replace print statements in AnalyticsService</name>
  <files>lib/services/analytics_service.dart</files>
  <action>Replace all print() statements with AppLogger calls:
1. Import AppLogger
2. Use AppLogger.analytics for analytics-specific logging
3. Maintain all information currently being printed
4. Use appropriate levels (INFO for normal operations, SEVERE for errors)

This file may have fewer print statements - replace any that exist with AppLogger equivalents.</action>
  <verify>File contains no print() statements (or none exist to replace)</verify>
  <done>AnalyticsService uses AppLogger.analytics for all logging, no print statements remain</done>
</task>

<task type="auto">
  <name>Task 3: Replace print statements in BackupService</name>
  <files>lib/services/backup_service.dart</files>
  <action>Replace all print() statements with AppLogger calls:
1. Import AppLogger
2. Use AppLogger.backup for backup operations
3. Use SEVERE for export/import failures, INFO for successful operations
4. Maintain timestamp and transaction count information in log messages

Example pattern:
- OLD: print('Failed to export data: $e')
- NEW: AppLogger.backup.severe('Failed to export data', error: e)</action>
  <verify>File contains no print() statements, all replaced with AppLogger calls</verify>
  <done>BackupService uses AppLogger.backup for all logging, no print statements remain</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] flutter run starts without errors
- [ ] grep -r "print(" lib/services/ returns no results
- [ ] All service files import and use AppLogger
- [ ] Services function correctly when tested manually
</verification>

<success_criteria>
- All print statements in services replaced with AppLogger
- Service layer uses structured logging consistently
- No errors introduced in service functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-logging-foundation/01-02-SUMMARY.md`:

# Phase 1 Plan 2: Service Logging Summary

**Replaced print statements with structured logging in all service files**

## Accomplishments

- DatabaseService migrated to AppLogger.database
- AnalyticsService migrated to AppLogger.analytics
- BackupService migrated to AppLogger.backup
- Consistent logging patterns across service layer

## Files Created/Modified

- `lib/services/database_service.dart` - Replaced print with AppLogger
- `lib/services/analytics_service.dart` - Replaced print with AppLogger
- `lib/services/backup_service.dart` - Replaced print with AppLogger

## Decisions Made

- Keep emoji prefixes in messages for visual clarity
- Use appropriate log levels (INFO for success, SEVERE for errors)
- Component-specific loggers (database, analytics, backup)

## Issues Encountered

None

## Next Step

Ready for 01-03-PLAN.md - Replace print statements in screens
